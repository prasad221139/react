# React application

# parcel
-Dev build   -> npx parcel index.html
-production build  -> npx parcel build index.html
-local server - 1234
-HMR hot module replacement
-it auto refreshes browser
-file matching algorithm -> written in C++
-gives faster builds -> done by caching
-it does image optimization
-it does file bundling
-it does file compress
-it does differential bundling -> supports older browsers
-tree shaking - removes unused code

# Browser-list
 -it used to cofigure which browser versions should be supported
 -we can configure based on country wise as well